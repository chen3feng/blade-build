language: python

addons:
  apt:
    sources:
    - kubuntu-backports
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.9
    - g++-4.9
    - ninja-build
    
script:
  - src/test/runalltests.sh
